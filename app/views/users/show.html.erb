<p id="notice"><%= notice %></p>

<% if user_signed_in? %>
  <p>
    <%= link_to 'Follow', follows_path({ follow: { following_id: @user.id }}), method: 'post'  %>
  </p>
<% else %>
  <p>
    You need to <%= link_to('Sign in', new_user_session_path) %> to follow this user
  </p>
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
