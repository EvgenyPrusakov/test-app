source 'https://rubygems.org'

ruby '2.5.1'

gem 'rails', '~> 5.2.0'                       # Rails Framework
gem 'pg'                                      # Pg is the Ruby interface to the PostgreSQL RDBMS.
gem 'puma', '~> 3.11'                         # Use Puma as the app server
gem 'bootstrap', '~> 4.1.1'                   # Bootstrap 4 Ruby Gem for Rails / Sprockets and Compass.
gem 'jquery-rails'                            # A gem to automate using jQuery with Rails
gem 'sass-rails', '~> 5.0'                    # Use SCSS for stylesheets
gem 'autoprefixer-rails'                      # Autoprefixer is a tool to parse CSS and add vendor prefixes to CSS rules.
gem 'uglifier', '>= 1.3.0'                    # Use Uglifier as compressor for JavaScript assets
gem 'devise'                                  # Flexible authentication solution for Rails with Warden
gem 'coffee-rails', '~> 4.2'                  # Use CoffeeScript for .coffee assets and views
gem 'turbolinks', '~> 5'                      # Turbolinks makes navigating your web application faster. Read more: https://github.com/turbolinks/turbolinks
gem 'andand'                                  # Maybe Monad in idiomatic Ruby.
gem 'bootsnap', '>= 1.1.0', require: false    # Reduces boot times through caching; required in config/boot.rb
gem 'simple_form'                             # Forms made easy for Rails! It's tied to a simple DSL, with no opinion on markup.

group :development do
  gem 'listen'                                # The Listen gem listens to file modifications and notifies you about the changes.
  gem 'annotate'                              # Annotate Rails classes with schema and routes info
  gem 'bullet'                                # help to kill N+1 queries and unused eager loading
  gem 'awesome_print'                         # Pretty print your Ruby objects with style -- in full color and with proper indentation
end

group :test do
  gem 'capybara', '>= 2.15', '< 4.0'          # Adds support for Capybara system testing and selenium driver
  gem 'selenium-webdriver'                    # A browser automation framework and ecosystem. https://www.seleniumhq.org
  gem 'chromedriver-helper'                   # Easy installation and use of chromedriver to run system tests with Chrome
end
